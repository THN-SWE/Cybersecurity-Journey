- The `cat` command is mostly used to quickly view content of a file. mostly for small files. 
- `head` and `tail` are other 2 commands to view long text files. only brings up first few lines or last few lines.