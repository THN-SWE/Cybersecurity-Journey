
> The `ifconfig` command stands for "interface configuration" and is used to display **network configuration** information.

![](../Assets/Screenshot%202026-03-01%20183118.png)
- eth0 is my primary network device.

> The `lo` device is referred to as the [[loopback]] device. It is a special network device used by the system when sending network-based data to itself.

> **The `ifconfig` command can also be used to temporarily modify network settings.**
> Typically these changes should be permanent, so using the `ifconfig` command to make such changes is fairly **rare**.

> The `ping` command is used to verify connectivity between two computers. It does this by sending packets to another machine on a network. If the sender receives a response it should be possible to connect to that machine.

![](../Assets/Screenshot%202026-03-01%20184044.png)
- I am pinging this IP address meaning, I am sending network packets to this machine to see if it responds back. 
- `-c` with this option i can decide how many packets to send otherwise it will keep sending ping packets. 

